[*
   $ENV{__EMB_EXTRATITLE}="configuration examples";
   $ENV{__EMB_COUNT}++; 
   if (1==$ENV{__EMB_COUNT}) {
      Execute('base.epl');
   } else {
*]

<p>Currently,  there's only the mc configuration available. Maybe I'll
add mutt later on (incl. mlnn/cron/procmail?).</p>


[*}*][# MUST BE AT EOF! #]
